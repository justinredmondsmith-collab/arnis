# Development RC1 freeze

Owning renderer #3. This records the exact development candidate for tiler #74,
#75, #76 and #77; it is not production support or an installation.

The immutable source ref is `tiler-v3.1.0-dev-rc1`, targeting
a3d39ca44a78a6539cc920f5f4b029818859cc21, on exact upstream Arnis v3.1.0
3918513acb4e5e9ef4332418531a7c444d2b5acf. The documentation commit holding this
record is not the binary's source commit.

The [candidate tuple](../contracts/rc/arnis-v3.1-development-rc1/rc-tuple.json)
binds source, lockfile, toolchain, feature set, binary, assets, profile, capability
report, master-grid schema, provider fixture and output layout. Follow the
[build and invalidation recipe](../contracts/rc/arnis-v3.1-development-rc1/BUILD-AND-INVALIDATION.md).

The earlier world-fix plan and validation predate the coastal delivery. Their
pending-coastal statements are superseded by the completed implementation and
review evidence in the coastal validation document. All 23 patch dispositions
are final. The separate Battery shoreline candidate still fails its DEM conflict
check and is not qualified. Empty capabilities deliberately prevent full consumer
admission until the dependent qualification work succeeds.

Any renderer, dependency, compiler, feature, ABI/profile or source-data change must
follow the invalidation rules and receive a new candidate identity where required.
No v2 reference, installed binary, production cache or world is changed by this freeze.

The three-file build pin adds Rust 1.97.0 locally/in CI and fixes native build time
with SOURCE_DATE_EPOCH=1788828777. Independent clone/target outputs match exactly:
`e6957b956396c288b1ac3017b618d897eabf96be828769b03b3efc131a2ed2db`.
Final pinned-source tests: 714 passed, six ignored. CLI/stock and frozen-provider
smokes pass against this exact artifact. See the tuple for the precise build
sequence and evidence hashes.
